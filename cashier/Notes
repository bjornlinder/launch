@RegEx Regular Expressions

"thats wicked pissah" =~ /wicked (pissah)?/
#if contains wicked, returns place in the string
"the computah is ovah there".gsub(/ah /,'er')
#replaces 'ah '
.gsub(/\Apacky/) #replaces word found at beginning of string
.gsub(/Apacky\z/) #replaces at end
"string".scan(/((packy)|(word2)|(word3))/).size

\d+
#d: must be digit. +: must be multiple characters.
\d+.\d{2}
#Any number to left, 2 to right.
\A\d+.\d{2}\z
#Any number chars to left, none to right.